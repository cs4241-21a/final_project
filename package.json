{
  "name": "final_project",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "engines": {
    "node": "v16.10.0"
  },
  "dependencies": {
    "base-64": "^1.0.0",
    "express": "^4.17.1",
    "luxon": "^2.0.2",
    "node-fetch": "^3.0.0",
    "npm-run-all": "^4.1.5",
    "path": "^0.12.7",
    "dotenv": "^10.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start-client": "cd client && npm run start",
    "dev": "run-p start-client start",
    "build": "cd client && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
